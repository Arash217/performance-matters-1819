<div id="countries">
    {{#each countries}}
        <a href="countries/{{alpha2Code}}">
            <div class="country">
                <img src="{{flag}}" alt="{{name}}">
                <p>{{name}}</p>
            </div>
        </a>
    {{/each}}
</div>